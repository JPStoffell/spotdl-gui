# Python dependencies installed via pip.
# GTK4 / libadwaita themselves come from Fedora system packages, not pip:
#   sudo dnf install gtk4 libadwaita python3-gobject
spotdl>=4.2

# Only needed by the Windows build (spotdl_ui/qt_app.py + qt_window.py),
# built via .github/workflows/build-windows.yml. Not installed on Linux,
# where the GTK front end (spotdl_ui/app.py + window.py) is used instead.
PyQt6>=6.7.0; sys_platform == "win32"
